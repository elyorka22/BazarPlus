# 📘 Инструкция: Как создать админа

## Способ 1: Через Supabase Dashboard (Самый простой)

### Шаг 1: Создать пользователя

1. Откройте **Supabase Dashboard**
2. Перейдите в **Authentication** → **Users**
3. Нажмите **"Add User"** (или **"Invite User"**)
4. Заполните форму:
   - **Email**: `admin@example.com` (или ваш email)
   - **Password**: придумайте надежный пароль
   - **Auto Confirm User**: ✅ включите эту опцию (чтобы не было проблем с подтверждением email)
5. Нажмите **"Create User"**
6. **Скопируйте User UUID** (он будет показан в списке пользователей)

### Шаг 2: Выполнить SQL код

1. Откройте **SQL Editor** в Supabase Dashboard
2. Откройте файл `create_admin_simple.sql` из проекта
3. **Замените** `'admin@example.com'` на email, который вы использовали при создании пользователя
4. **Замените** `'Admin User'` на имя админа (например, `'Главный Администратор'`)
5. Нажмите **"Run"** (или Ctrl+Enter)

### Шаг 3: Проверить результат

После выполнения SQL кода вы увидите результат запроса SELECT - это список всех админов. Убедитесь, что ваш пользователь там есть с ролью `'admin'`.

---

## Способ 2: Если знаете UUID пользователя

Если вы уже знаете UUID пользователя (например, из списка пользователей):

1. Откройте **SQL Editor**
2. Используйте альтернативный вариант в файле `create_admin_simple.sql`:
   - Раскомментируйте блок с комментарием "АЛЬТЕРНАТИВНЫЙ ВАРИАНТ"
   - Замените `'ВАШ_UUID_ЗДЕСЬ'` на реальный UUID
   - Замените email и имя
   - Выполните код

---

## Способ 3: Через Supabase CLI (для продвинутых)

Если у вас установлен Supabase CLI:

```bash
# Подключитесь к проекту
supabase link --project-ref your-project-ref

# Выполните SQL файл
supabase db execute -f create_admin_simple.sql
```

---

## Проверка: Как убедиться, что админ создан?

Выполните этот SQL запрос:

```sql
SELECT 
  id,
  email,
  name,
  role,
  created_at
FROM public.user_profiles 
WHERE role = 'admin';
```

Если вы видите вашего пользователя с ролью `'admin'` - всё готово! 🎉

---

## Проблемы и решения

### Проблема: "User not found"
- **Решение**: Убедитесь, что вы создали пользователя в Authentication → Users перед выполнением SQL

### Проблема: "Email not confirmed"
- **Решение**: В SQL коде есть команда UPDATE, которая подтверждает email. Если проблема осталась, выполните:
  ```sql
  UPDATE auth.users
  SET email_confirmed_at = NOW()
  WHERE email = 'ваш_email@example.com';
  ```

### Проблема: "Cannot insert - RLS policy violation"
- **Решение**: Убедитесь, что вы выполнили все миграции, особенно `009_fix_user_registration.sql`

---

## Готово! ✅

После выполнения всех шагов:
1. Войдите на сайт с email и паролем админа
2. Вы должны увидеть панель администратора
3. Если не видите - проверьте, что роль действительно `'admin'` в базе данных

